# ============================================================
# 2026 世界杯冠军预测系统 — Python 依赖
# ============================================================

# ── Web 框架 ──
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# ── 数据库 ──
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary>=2.9.9

# ── 数据验证 ──
pydantic==2.5.3
pydantic-settings==2.1.0

# ── 数据处理 ──
pandas==2.2.0
scipy==1.12.0
numpy==1.26.3

# ── 机器学习（CPU-only，不安装 CUDA 依赖）──
# 注意：Render 部署环境为 CPU，使用 --extra-index-url 安装 CPU 版 PyTorch
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu
scikit-learn>=1.3.0
xgboost>=2.0.0

# ── LangChain（固定版本，避免 pip backtracking）──
langchain==0.2.17
langchain-core==0.2.43
langchain-community==0.2.19
langchain-text-splitters==0.2.4
langsmith==0.1.147

# ── 向量数据库 ──
chromadb>=0.4.22

# ── LLM API ──
openai>=1.10.0
zhipuai>=2.0.0

# ── 定时任务 ──
apscheduler>=3.10.0

# ── 数据采集 ──
requests>=2.31.0
beautifulsoup4>=4.12.0
playwright>=1.40.0

# ── 工具 ──
python-dotenv>=1.0.0

# ── 前端（Streamlit Dashboard）──
streamlit>=1.30.0
plotly>=5.18.0
altair>=5.2.0
openpyxl>=3.1.0
